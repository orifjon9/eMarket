@model IEnumerable<eMarket.DTO.Module.ProductDTO>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="row">
    <div class="col-xs-12">
        @foreach (var item in Model)
        {
           <h1>@item.Name</h1>
        }

    </div>
</div>

